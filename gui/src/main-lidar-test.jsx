import React from 'react'
import ReactDOM from 'react-dom/client'
import LidarTest from './LidarTest'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LidarTest />
  </React.StrictMode>,
)
